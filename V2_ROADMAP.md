# LumiAgent — TRAE AI 创造力大赛参赛规划

> 参赛作品：LumiAgent — 为老旧设备而生的 AI 编程调度器
> 参赛理念：算力平权，让所有设备都能平等获得 AI 编程辅助
> 参赛者：Kai，高一学生，无独显设备，从零独立开发

---

## 零、30 秒电梯演讲

**问题**：AI 时代来了，但很多人（包括我）的电脑没有 GPU，跑不动大模型。

**方案**：93 行 Python 代码，一个"休息机制"，让老旧设备也能用本地 AI 编程助手。

**为什么是我**：因为这个问题是真实的——我就是那个用无独显电脑挣扎着学 AI 的人。

**为什么现在**：TRAE AI 创造力大赛不只看技术有多强，更看想法有多真。这个项目没什么黑科技，但它是真的。

---

## 一、V1 已实现功能总结

| 功能 | 状态 | 实现方式 |
|:---|:---|:---|
| 命令行交互 | ✅ | `while True` 循环 + `input()` 读取 |
| 本地 Ollama 调用 | ✅ | `requests.post()` → `/api/generate` |
| 休息机制 | ✅ | `time.sleep(30)` + 休眠提示 |
| 错误处理 | ✅ | `try/except` 捕获连接异常、JSON 解析异常、未知异常 |
| 退出命令 | ✅ | `exit` / `quit` / `退出` 判断 + `break` |
| /help 命令 | ✅ | 打印项目定位和使用方法 |

**V1 技术亮点**：
- 93 行代码实现完整 CLI 交互
- 面向 Python 初学者的逐行中文注释
- 体现"算力平权"理念的欢迎语和休眠关怀
- 预留 V2 扩展点注释标记

---

## 二、已知缺陷与设计限制

### 2.1 设计限制（非 bug，V1 铁律故意限制）

| 限制 | 说明 | V1 决策原因 |
|:---|:---|:---|
| 无上下文记忆 | 每次对话独立，无法理解"上一个问题" | V1 铁律：禁止实现多轮对话 |
| 单模型 | 仅支持本地 Ollama，无云端回退 | V1 铁律：禁止云端模型接入 |
| 无任务规划 | 直接调用模型，不分析意图和拆分任务 | V1 铁律：禁止任务规划器 |

### 2.2 实现缺陷（需要在 V2 解决）

| 缺陷 | 影响 | 根因 |
|:---|:---|:---|
| **小模型幻觉** | deepseek-r1:1.5b 知识有限，回答可能不准确 | 模型参数量限制（1.5B） |
| **无流式输出** | 长回答需要等待完整生成才能显示 | `stream: False` 设置 |
| **固定休眠时间** | 无论回答长短都等待 30 秒 | 未根据任务复杂度动态调整 |

---

## 三、V2 核心目标：双模型调度宪法

**一句话：本地守门人判断任务难度 → 简单留本地 → 复杂升云端。**

### 3.1 V2 分阶段路线图

#### 阶段一：最小双模型调度（P0 优先）

| 任务 | 说明 |
|:---|:---|
| 云端 API 接入 | 实现 DeepSeek-V4-Pro 调用（API Key 配置） |
| 本地守门人判断 | 让 1.5b 模型判断"这个任务我能不能搞定" |
| 调度逻辑 | 本地模型认为搞不定 → 升级云端 |
| 回退机制 | 云端不可用 → 降级回本地 |

#### 阶段二：上下文记忆（P1）

| 任务 | 说明 |
|:---|:---|
| 对话历史存储 | 用列表存储最近 N 轮对话 |
| Prompt 拼接 | 把历史对话拼入当前请求 |
| 历史截断 | 超出 Token 限制时截断最早对话 |

#### 阶段三：任务规划层（P2）

| 任务 | 说明 |
|:---|:---|
| 意图分析 | 轻量级关键词匹配判断任务类型 |
| 任务框架生成 | 云端模型生成步骤分解 |
| 用户确认机制 | 执行前展示框架，用户确认后执行 |

#### 阶段四：资源优化（P3）

| 任务 | 说明 |
|:---|:---|
| 动态休眠时间 | 根据回答长度动态调整休眠 |
| 流式输出 | 开启 `stream: True`，逐字显示回答 |
| 多模型适配层 | 支持 Claude / GPT / Qwen 等多云端模型 |

### 3.2 V2 最小可行原型（MVP）流程

```
用户输入 "写一个复杂的 Python 爬虫"
    ↓
本地模型（1.5b）判断：这个任务超出我的能力
    ↓
升级到云端模型（DeepSeek-V4-Pro）
    ↓
返回完整代码
    ↓
休息机制（动态时间）
    ↓
等待下一次输入（带上下文记忆）
```

---

## 四、V1 → V2 演进路线（比赛版）

### 4.1 V1 现状

```
┌──────────────────────────────────────────────────┐
│                    V1 核心能力                     │
├──────────────────────────────────────────────────┤
│  ✅ 命令行交互           ✅ Ollama 模型调用        │
│  ✅ 30 秒休息机制        ✅ /help 命令             │
│  ✅ 三层异常处理          ✅ 93 行极简代码          │
│  ⚠️ 小模型可能犯错        ⚠️ 无系统提示词          │
│  ⚠️ 固定休眠时长          ⚠️ 仅 CLI 交互           │
└──────────────────────────────────────────────────┘
```

### 4.2 V2 发展方向

| 方向 | 解决什么问题 | 一句话说明 | 预计代码增量 |
|:---|:---|:---|:---|
| **双模型调度** | 本地模型能力有限 | 简单问题本地解决，复杂问题自动升级云端 | +40 行 |
| **内容自检** | 模型偶尔犯错 | 答完后自动检查一遍，可疑时提醒用户 | +30 行 |
| **上下文记忆** | 每次对话都是"陌生人" | 记住最近对话，让模型理解上下文 | +25 行 |
| **动态休眠** | 固定 30 秒太死板 | 简单问题少休息，复杂问题多休息 | +20 行 |
| **HTTP API** | 只能命令行用 | 加一个 `/ask` 接口，其他工具也能调用 | +25 行 |
| **多模型切换** | 只能用一个模型 | 支持切换不同模型，对比效果 | +20 行 |

---

## 五、作品创新点（评委视角）

### 创新点 1：休息机制

> 所有 AI 工具都在追求"更快更强"，LumiAgent 反其道而行——主动让设备休息。

**为什么是创新**：
- 主流 AI 工具追求高并发、低延迟，但老旧设备根本无法承受持续负载
- 休息机制不是技术妥协，而是**设计哲学**——承认硬件的局限，并与之共存
- 每次回答后主动休眠 30 秒，让操作系统有机会卸载模型释放内存

### 创新点 2：极简主义

> 93 行代码，1 个依赖，每行都有中文注释——初学者也能看懂。

**为什么是创新**：
- 大多数 AI 项目动辄几千行代码、几十个依赖，学习门槛极高
- LumiAgent 证明了：**做 AI 不需要复杂的技术栈**
- 每行代码都有中文注释，这是"写给初学者的 AI 入门课"

### 创新点 3：真实问题驱动

> 我不是为了参加比赛才做这个项目——我是为了解决自己的问题。

**为什么是创新**：
- 大部分参赛作品从"技术能做啥"出发，LumiAgent 从"我需要啥"出发
- 没有高端显卡是真实困境，不是虚构的需求
- 这个项目解决的是一类人的问题：学生、小城市、老旧设备

---

## 六、技术亮点（答辩准备）

### 6.1 架构设计

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  CLI 入口 │────▶│ LumiAgent │────▶│  Ollama  │
│  main()   │     │  核心调度  │     │  本地模型 │
└──────────┘     └─────┬────┘     └──────────┘
                       │
                  ┌────▼────┐
                  │ rest()   │
                  │ 休息机制  │
                  └─────────┘
```

### 6.2 可能被问到的技术问题

| 问题 | 回答准备 |
|:---|:---|
| "为什么用 `generate` 端点而不是 `chat` 端点？" | V1 追求极简，`generate` 端点更简单直接。V2 会切换到 `chat` 端点引入系统提示词 |
| "休息机制是否真的能释放内存？" | Python 的 `time.sleep()` 期间，操作系统可以将不活跃的内存页换出。这是利用 OS 的惰性内存管理策略 |
| "为什么不用 GPU 加速？" | 这个项目的核心定位就是"给没有 GPU 的人用"。如果用了 GPU，就违背了算力平权的初衷 |
| "93 行代码能做什么？" | 调度系统。AI 的复杂计算交给 Ollama 和模型，LumiAgent 只负责"调度"——什么时候调用、什么时候休息 |
| "和 ChatGPT / Copilot 比有什么优势？" | 完全离线、完全免费、数据不离开本地。它解决的是"能用"的问题，不是"更好"的问题 |

---

## 七、用户价值与社会影响

### 7.1 目标用户

| 画像 | 痛点 | 解决方案 |
|:---|:---|:---|
| 学生（无独显笔记本） | 想学 AI 编程但跑不动大模型 | 纯 CPU 推理，本地运行 |
| 偏远地区学校 | 网络不稳定，无法用云端 AI | 完全离线，无需联网 |
| 编程初学者 | 看不懂复杂的 AI 项目代码 | 93 行中文注释，逐行可读 |
| 老旧设备用户 | 电脑配置低，开 IDE 都卡 | 极简 CLI，资源占用最小 |

### 7.2 一句话社会价值

> 不让硬件成为 AI 的准入门槛。

---

## 八、参赛材料清单

| 序号 | 材料 | 状态 | 备注 |
|:---|:---|:---|:---|
| 1 | GitHub 仓库 | ✅ 已完成 | https://github.com/k3234/lumiagent |
| 2 | README.md | ✅ 已完成 | 含定位、快速开始、工作原理、开发者故事 |
| 3 | DEV_LOG.md | ✅ 已完成 | 完整开发日志，4 天时间线 |
| 4 | V2_ROADMAP.md | ✅ 本文件 | 比赛版演进规划 + 双模型调度宪法 |
| 5 | demo.html | ✅ 已完成 | 项目展示页，含运行环境说明 |
| 6 | 30 秒演示视频 | ⬜ 待制作 | 按脚本录制 |
| 7 | 论坛作品帖 | ⬜ 待撰写 | 按 TRAE 论坛模板 |

---

## 九、V2 时间线

| 阶段 | 时间 | 任务 |
|:---|:---|:---|
| 比赛期 | 2026-06 | 完善参赛材料，提交作品 |
| V2-alpha | 2026 暑假 | 双模型调度（本地守门人 + 云端升级） |
| V2-beta | 高二上学期 | 上下文记忆 + 内容自检 + 动态休眠 |
| V2 正式版 | 高二下学期 | HTTP API + 多模型适配 + 流式输出 |

---

## 十、V3 愿景（远期）

| 方向 | 说明 |
|:---|:---|
| Web 可视化界面 | 将调度过程可视化，展示任务拆解和休息机制 |
| Agent 群协作 | 多个 Agent 协同，实现"难-简-难-简"交叉负载均衡 |
| 社区插件生态 | 开放插件接口，让其他开发者贡献功能模块 |
| 跨平台 GUI | 让非技术用户也能通过图形界面使用 |

---

## 十一、最后的话

这不是一个技术多么厉害的项目。

它只是 93 行 Python 代码，调用一个本地模型，加一个 30 秒的休息。

但它是真实的。

真实的问题：没有好设备。
真实的解法：用最简单的方式调用 AI。
真实的局限：模型会犯错，我公开了它。

如果 TRAE AI 创造力大赛在找"有想法、有温度、有未来"的作品——我希望 LumiAgent 能让你看到，一个高一学生，用一台没有独显的电脑，也可以做出属于自己的 AI 工具。

**算力平权，从 93 行代码开始。**

---

*最后更新：2026-06-27*
*为 TRAE AI 创造力大赛准备*