# LumiAgent 开发日志

> 记录每一个版本的生命周期

---

## V1 — 算力平权的起点

### 2026-06-19 · 规则文件创建

今天创建了项目的"技术宪法"——规则文件。

这一步很重要。我不想边做边改规则，那样最后会变成一团乱麻。所以先花了一点时间把核心理念写下来：

- 算力平权：让没有高端设备的同学也能用 AI
- 休息机制：老设备不能一直跑，必须让它休息
- 极简主义：不堆功能，只做一件事并且做好

这些规则会成为之后每一个决定的基准线。

---

### 2026-06-22 · V1 最小原型完成

代码量：**80 行**

核心功能全部跑通：
- 调用 Ollama API（使用 `requests.post`）
- 接收用户输入
- 返回模型回答
- 30 秒休眠机制

最难的部分不是调 API，而是理解"为什么老设备需要休息"。一开始我以为只要能调用模型就行了，后来才发现内存才是瓶颈。模型加载后会一直占用 RAM，只有主动等待，操作系统才有机会卸载它。

这就是"休息机制"的由来。

---

### 2026-06-23 · /help 功能加入，V1 正式完工

加了一个小功能：`/help` 命令，输出使用说明。

看起来是小事，但其实很重要——一个工具如果没人会用，就等于不存在。

同时整理了代码注释，确保每一行都有中文解释。初学者（比如我）读代码最怕的就是不知道某行在干嘛。

**V1 正式发布。**

---

### 2026-06-24 · 准备 TRAE AI 创造力大赛参赛材料

决定参加 TRAE AI 创造力大赛。

这个项目不是什么高大上的技术突破，它只是解决了一个真实的问题：我们学校很多人电脑很旧，跑不了那些需要 GPU 的 AI 工具。

我希望把这个经历写进参赛材料里——不是因为它有多厉害，而是因为它是真的。

开发日志整理完成，README 重写，仓库结构规范化。

**准备提交。**

---

## 技术细节

- **API 地址**：Ollama 本地推理端点（需提前运行 `ollama serve`）
- **调用方式**：`/api/generate` 端点，非流式输出
- **超时设置**：60 秒
- **依赖数量**：1 个（`requests`）
- **代码行数**：93 行（含空行和注释）

---

*Last updated: 2026-06-24*
